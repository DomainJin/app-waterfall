// Placeholder — audio waveform + offset slider, synced to master timeline.
// Implemented in a later phase.
export function AudioEditor() {
  return <section className="panel" data-panel="audio-editor" />;
}
