// Placeholder — LED matrix editor (downsample, IC9803 encode, stream).
// Implemented in a later phase (spec file 03).
export function LedEditor() {
  return <section className="panel" data-panel="led-editor" />;
}
